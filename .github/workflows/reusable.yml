name: Reusable workflow
on: 
  workflow_call:
    inputs:
      artifact-description:
         description: "Hello"
         default: dist
         type: string
jobs:
 deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Get build artifacts
        uses: actions/download-artifact@v3
        with:
          name: ${{inputs.artifact-description}}
      - name: Output contents
        run: ls
      - name: Deploy
        run: echo "Deploying..."